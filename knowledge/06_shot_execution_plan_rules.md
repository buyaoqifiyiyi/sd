# Legacy Compatibility — Shot Execution Plan Rules

本文件只供旧项目恢复时解释既有文字 Shot Execution Plan。它不绑定 STATE-07，不参与新项目路由。

新项目使用 `STATE-06 Detailed Shot Design → STATE-07 Clip Production`。旧 Shot Execution Plan只可作为Detailed Shot Design的只读兼容输入，不得恢复固定中间阶段或改变STATE-08模板。
